{
  "name": "@wt/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "《巫师飞塔》前端组件层 — Phase 3 MVP（当前为半成品，未编译未接线，Phase C 重写）",
  "license": "UNLICENSED",
  "dependencies": {
    "@wt/shared": "workspace:*",
    "@wt/engine": "workspace:*",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.0"
  },
  "comments": {
    "typecheck": "本包当前不参与根 `pnpm typecheck`（未配置 typecheck script / tsconfig）。内容基于不存在的旧 API，待 Phase C 基于真实引擎 API 重写后再接入编译链。"
  }
}
